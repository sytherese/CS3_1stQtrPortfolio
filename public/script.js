let age = 14;
let maxAge = 100;
let numPerDay = 2;
let totalRequired = (numPerDay * 365) * (maxAge - age);

document.write("You will need " +totalRequired + " red velvet cupcakes to last you until the ripe old age of " +maxAge );

